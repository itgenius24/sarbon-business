include:
  - .gitlab/ci/*.gitlab-ci.yml

stages:
  - build
  - deploy
  - rollback

build-test:
  stage: build
  extends: .build_template
  variables:
    ENV_TAG: test
    DOCKERFILE: Dockerfile
  only:
    - staging

build-prod:
  stage: build
  extends: .build_template
  variables:
    ENV_TAG: latest
    DOCKERFILE: Dockerfile
  only:
    - master

deploy-test:
  stage: deploy
  extends: .deploy_template
  variables:
    NAMESPACE: furgo-staging
    VALUES_FILE: .helm/values-test.yml
    K8SCONFIGJSON: $FURGO_KUBECONFIG
  only:
    - staging

deploy-prod:
  stage: deploy
  extends: .deploy_template
  variables:
    NAMESPACE: furgo
    VALUES_FILE: .helm/values-prod.yml
    K8SCONFIGJSON: $FURGO_KUBECONFIG
  only:
    - master

rollback-test:
  stage: rollback
  extends: .rollback_template
  variables:
    NAMESPACE: furgo-staging
    K8SCONFIGJSON: $UCODE_HZN_KUBECONFIG
  when: manual

rollback-prod:
  stage: rollback
  extends: .rollback_template
  variables:
    NAMESPACE: furgo
    K8SCONFIGJSON: $UCODE_HZN_KUBECONFIG
  when: manual

